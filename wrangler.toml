name = "likely2x-tapeout-proxy"
main = "worker/tapeout-proxy.ts"
compatibility_date = "2026-09-22"

[triggers]
crons = ["*/5 * * * *"]

# Bind a KV namespace as SNAPSHOTS and a D1 database as DB in the Cloudflare
# dashboard (or add their account-generated IDs here before deploying).
